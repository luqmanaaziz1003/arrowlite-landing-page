## What changed

<!-- A sentence or two. What does this PR do, and why? -->

## Pages affected

<!-- e.g. /services, /services/[slug] — or "none, config only" -->

## Checklist

- [ ] I ran `npm run lint`, `npm run typecheck` and `npm run build` locally
- [ ] I opened the affected pages in the browser and they look right
- [ ] I checked the pages at mobile width as well as desktop
- [ ] Content changes were made in `lib/content/` or `lib/site.ts`, not hardcoded in a page

## Preview

<!-- Vercel posts a preview URL on this PR automatically. Paste anything
     reviewers should look at specifically. -->
